// Injected by Vite's `define` (see vite.config.ts).
declare const __VERSION__: string;

interface Window {
  customCards?: Array<Record<string, unknown>>;
  customBadges?: Array<Record<string, unknown>>;
}
